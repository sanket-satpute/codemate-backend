spring:
  main:
    lazy-initialization: true
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://codemate_db_user_sanket:${MONGODB_PASSWORD}@cluster0.9sqz4vx.mongodb.net/codescope?retryWrites=true&w=majority&appName=Cluster0}

server:
  port: ${PORT:8080}

file:
  upload-dir: /tmp/uploads

frontend:
  origins: ${FRONTEND_ORIGINS:${FRONTEND_ORIGIN:https://codemate-five.vercel.app,https://codemate-git-main-ssatpute738-2203s-projects.vercel.app,https://codemate-78zgnunry-ssatpute738-2203s-projects.vercel.app}}
  origin-patterns: ${FRONTEND_ORIGIN_PATTERNS:https://codemate-*.vercel.app}

app:
  redis:
    enabled: ${APP_REDIS_ENABLED:false}

project:
  id:
    backfill:
      enabled: ${PROJECT_ID_BACKFILL_ENABLED:false}

logging:
  level:
    root: INFO
    com.codescope.backend: INFO

management:
  endpoint:
    health:
      probes:
        enabled: true
        add-additional-paths: true

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
